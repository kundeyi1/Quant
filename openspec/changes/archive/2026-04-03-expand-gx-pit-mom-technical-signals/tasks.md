## 1. 测试入口 `pit_mom.py` 搭建与内建逻辑实现

- [ ] 1.1 创建 `pit_mom.py` 文件，继承 `GXPITMomTester`。
- [ ] 1.2 在 `pit_mom.py` 内部实现物理因子计算逻辑（如 `_calc_physical_metrics`），避免修改 `factors/` 文件夹。
- [ ] 1.3 在 `pit_mom.py` 内部实现两阶段触发引擎，逻辑为：物理 Setup + 3% 当日涨幅确认。

## 2. 行业选择逻辑与动量分配

- [ ] 2.1 在 `pit_mom.py` 中重写或新增行业动量分配逻辑，确保仅在信号触发日提取当日行业表现。
- [ ] 2.2 确保 `DataManager` 调用方式在 `pit_mom.py` 中保持原样，仅在本地处理数据切片。

## 3. 回测运行与全流程验证

- [ ] 3.1 封装调用 `SparseSignalTester` 的循环，确保除了信号触发点的行业动量分配外，其余系统逻辑与 `gx_pit_mom` 保持一致。
- [ ] 3.2 针对 `000985` 运行 `price_near_high` + `3%` 动量确认测试，验证结果生成的目录结构。
- [ ] 3.3 验证物理指标在整个回测区间内的稳定性，并生成中信一、二级行业的回测对照报告。

## 4. 验证与全流程测试

- [ ] 4.1 针对 `000985` 运行 `price_near_high` + `3%` 动量确认测试，验证结果生成的目录结构。
- [ ] 4.2 验证物理指标在整个回测区间内的稳定性（Stationary Check），确保无数据前瞻或计算偏差。
- [ ] 4.3 生成中信二级行业的回测报告，并与原始 `gx_pit_mom.py` 结果进行对比分析。
